整理 overlay/tools + 清掉误提交的第三方代码

清掉第三方代码
  上一提交把 overlay/tools/_dump/ 带进去了 —— 那是为了解决
  「歌单导入」问题，从 imgcache.gtimg.cn 和 y.qq.com 扒下来的
  腾讯 JS 源码（music.js 182KB、aq_common.js 等）。
  属于第三方版权代码，public 仓库不该有。
    · 已从 git 移除并删除本地文件
    · .gitignore 加了 overlay/tools/_dump/，防止以后再生成

整理目录
  这轮调研往 overlay/tools 里塞了 41 个脚本，按用途分三组：

  playlist/          5 个成品工具
    playlist.mjs         读歌单 / 随机抽歌 / 查歌词
    playlist-stats.mjs   语种分布统计
    export-split.mjs     按语种拆分导出
    make-slices.mjs      切成可粘贴的小片
    resolve-share.mjs    解析分享短链

  api/               4 个排查工具
    probe-apis.mjs       ★ 接口诊断（区分限流和故障）
    stress-switch.mjs    ★ 快速切歌压测
    qq-session.mjs       从调试 Edge 取登录态
    launch-debug-edge.ps1 起带调试端口的 Edge

  api/_archive/      13 个归档
    逆加密、抓包、定位接口那轮的中间产物。不用跑了，
    留着是为了以后再碰类似问题能参考思路。

  删除 13 个一次性探针（抓包、试接口、UI 自动化那批）

新增
  overlay/tools/README.md     说明这三组工具怎么用
  overlay/tools/tidy.mjs      整理脚本（一次性，已用完）
  overlay/tools/fix-paths.mjs 修路径引用（一次性，已用完）

验证（整理后重跑）
  接口探针        6/6
  歌单工具        正常
  压测            正常
  冒烟测试        44/44
  计时器单测      40/40
